#!/usr/bin/python3
def number_keys(a_dictionary):
    """number_keys

    Args:
        a_dictionary:

    Return:
        The number of key in a dictionary
    """
    num_of_key = len(a_dictionary.keys())

    return num_of_key
